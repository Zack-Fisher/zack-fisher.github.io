//controller for constantly updating statistics
//comp status, demon status, etc

//only call on update
function renderCompStatus(compDiv){

}

function renderDemonList(demonDiv){

}

//all in percentages
var compStatus = {
    cpu: 0.0,
    ram: 0.0,
};

var demonList = {
    demons: [],
};